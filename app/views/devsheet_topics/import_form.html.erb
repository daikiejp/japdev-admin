<% content_for :title, "セクションをインポート - #{@topic.name} - JapDev" %>

<div class="max-w-3xl mx-auto">
  <div class="mb-8">
    <%= link_to "← #{@topic.name}に戻る",
    devsheet_category_devsheet_topic_path(@category, @topic),
    class: "text-emerald-600 hover:text-emerald-700 font-medium" %>
  </div>

  <div class="mb-8 bg-white rounded-xl p-8 shadow-sm border border-gray-100">
    <h1 class="text-3xl font-bold text-gray-900 mb-2">セクションをインポート</h1>
    <p class="text-gray-600 mb-8">JSONファイルからセクションとコードブロックをインポートします</p>

    <%= form_with url: import_sections_devsheet_category_devsheet_topics_path(@category, topic_id: @topic.id),
                  method: :post,
                  multipart: true,
                  class: "space-y-6" do |form| %>

      <div>
        <%= form.label :json_file, "JSONファイル", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= form.file_field :json_file,
                            accept: ".json",
                            class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500" %>
        <p class="mt-2 text-sm text-gray-500">
          セクションデータを含むJSONファイルを選択してください（例: flexbox.json）
        </p>
      </div>

      <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
        <h3 class="font-medium text-blue-900 mb-2">📋 JSONフォーマット</h3>
        <p class="text-sm text-blue-800 mb-2">JSONファイルは以下の構造である必要があります：</p>
        <pre class="bg-white p-3 rounded text-xs overflow-x-auto"><code>[
  {
    "id": "section-slug",
    "title": "セクションタイトル",
    "description": "説明",
    "codeBlocks": [
      {
        "title": "コードタイトル",
        "language": "html",
        "filename": "example.html",
        "code": "コード内容",
        "highlightLines": [1, 2, 3],
        "tabs": [
          {
            "label": "HTML",
            "code": "コード",
            "language": "html"
          }
        ]
      }
    ]
  }
]</code></pre>
      </div>

      <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
        <h3 class="font-medium text-yellow-900 mb-2">⚠️ 注意事項</h3>
        <ul class="text-sm text-yellow-800 space-y-1 list-disc list-inside">
          <li>同じslug（id）のセクションが存在する場合は上書きされます</li>
          <li>インポート中にエラーが発生した場合、すべての変更がロールバックされます</li>
          <li>大きなファイルの場合、処理に時間がかかる場合があります</li>
        </ul>
      </div>

      <div class="flex gap-3">
        <%= form.submit "インポート開始",
                        class: "px-6 py-3 bg-gradient-to-r from-emerald-500 to-teal-600 text-white rounded-lg font-medium hover:shadow-lg transition-all cursor-pointer" %>
        <%= link_to "キャンセル",
                    devsheet_category_devsheet_topic_path(@category, @topic),
                    class: "px-6 py-3 bg-gray-200 text-gray-700 rounded-lg font-medium hover:bg-gray-300 transition-all" %>
      </div>
    <% end %>
  </div>

  <div class="bg-gray-50 rounded-xl p-6 border border-gray-200">
    <h2 class="text-xl font-bold text-gray-900 mb-4">📚 サンプルファイル</h2>
    <p class="text-gray-600 mb-4">以下のファイル構造を参考にしてください：</p>

    <div class="space-y-3">
      <div class="bg-white p-4 rounded-lg border border-gray-200">
        <div class="flex items-center justify-between">
          <div>
            <p class="font-medium text-gray-900">fundamentals.json</p>
            <p class="text-sm text-gray-500">基本構造、よく使うタグ、フォームのセクション</p>
          </div>
        </div>
      </div>

      <div class="bg-white p-4 rounded-lg border border-gray-200">
        <div class="flex items-center justify-between">
          <div>
            <p class="font-medium text-gray-900">flexbox.json</p>
            <p class="text-sm text-gray-500">Flexboxの基本、配置とスペースのセクション</p>
          </div>
        </div>
      </div>

      <div class="bg-white p-4 rounded-lg border border-gray-200">
        <div class="flex items-center justify-between">
          <div>
            <p class="font-medium text-gray-900">grid.json</p>
            <p class="text-sm text-gray-500">CSS Gridレイアウトのセクション（タブ付き）</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
